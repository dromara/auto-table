/**
 * AutoTable SQLServer 策略模块。
 *
 * <p>通过 SPI 注册到核心引擎，提供 SQLServer 的自动建表/改表能力。</p>
 * <p>遵循 {@code Strategy + DatabaseBuilder + builder/ + data/ + mapper/} 的对称结构。</p>
 *
 * @author don
 */
package org.dromara.autotable.strategy.sqlserver;
